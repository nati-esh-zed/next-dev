{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun --env-file='../.env' --watch --hot run index.ts",
    "start": "bun --bun --env-file='../.env' --watch --hot run index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^25.4.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@bepalo/cache": "^1.0.3",
    "@bepalo/jwt": "^2.0.9",
    "@bepalo/router": "^1.9.28",
    "@bepalo/time": "^1.2.11"
  }
}
